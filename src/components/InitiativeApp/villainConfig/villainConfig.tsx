import { Villain } from '../../../models/creatures/villain';

export type VillainConfigProps = {
  villains: Villain[];
};

const VillainConfig = ({}: VillainConfigProps) => {
  return <></>;
};

export default VillainConfig;
