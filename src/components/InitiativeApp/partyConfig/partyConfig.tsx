import { Villain } from '../../../models/creatures/villain';

export type PartyConfigProps = {
  villains: Villain[];
};

const PartyConfig = ({}: PartyConfigProps) => {
  return <></>;
};

export default PartyConfig;
